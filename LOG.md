# День 1 (2026-03-31)

        Сделал:
    -- Добавил проект с TS,ESLint,Husky,Prettier
    -- Настроил базовую структуру по слоям
    -- Создал простой Express с чекпоинтом '/health'
    -- Настроил Git,создав первый commit

        Ответ на свой вопрос:
    -- Выбрал tsx watch вместо nodemon, потому что tsx быстрее и не требует компиляции перед запуском
    --strict: true в tsconfig — чтобы TypeScript ловил ошибки на этапе написания кода
    --Husky + lint-staged — чтобы в репозиторий не попадал код с ошибками линтера

        Сложности
    --Сначала не понял разницу между tsx и ts-node. Оказалось, tsx быстрее и использует esbuild, а ts-node — классический компилятор. Для разработки tsx удобнее.


# День 2 (2026-04-01)

        Сделал
    - Создал Dockerfile и docker-compose.yml
    - Поднял PostgreSQL и Redis в контейнерах
    - Установил Prisma, настроил подключение

        Ответ на свой вопрос:
    - Docker изолирует окружение — не нужно ставить PostgreSQL и Redis на компьютер
    - volumes — чтобы код обновлялся без пересборки образа
    - depends_on — чтобы база стартовала раньше API

        Сложности
    - Тяжело запомнить за что отвечает докерФайл и докерКомпоз
    
# День 3 (2026-04-02)
        Сделал
    - Создал schema.prisma(бд) с User, Post, Follow, Like
    - Сделал измение таблицы через Prisma
    - Написал prisma.ts который создает один общий файл переходник
    - Добавил проверку на то,что жив ли БД

        Ответ на свой вопрос:
    -@unique нужен для быстрого поиска
    -@default(cuid()) генерирует уникальный айди
    -onDelete: Cascade при удалении пользователя удаляет все что с ним связано
    -создал один экзмпляр что бы не создавать лишних файлов

        Сложности
    -Поднять БД 
    -Создать бд с User, Post, Follow, Like

# День 4 (2026-04-02)   
        Сделал

Создал репозитории: user.repository.ts, post.repository.ts, like.repository.ts, follow.repository.ts

Каждый репозиторий содержит только прямые запросы к Prisma

Никакой бизнес-логики — только CREATE, READ, UPDATE, DELETE

Ответ на свой вопрос:

Репозиторий — прослойка между сервисом и БД

Если придётся менять Prisma на другое ORM, меняю только репозитории

Сервисы не знают, какая БД используется — только вызывают методы репозитория

Сложности:

Долго решал, какие методы нужны в каждом репозитории

Забывал про select и возвращал лишние поля (пароль, createdAt)

Путался между findMany и findUnique

День 5 (2026-04-03)
Сделал:

Написал сервисы: auth.service.ts, post.service.ts, like.service.ts, follow.service.ts

Вся бизнес-логика в сервисах: проверки, валидация, вызовы репозиториев

JWT генерация и bcrypt хеширование

Ответ на свой вопрос:

Сервис НЕ должен знать про HTTP (req, res)

Сервис возвращает данные или кидает ошибку

Контроллер только вызывает сервис и отправляет ответ

Сложности:

Не знал, как пробросить userId из middleware в контроллер → req.user

Забыл хешировать пароль перед сохранением в БД

День 6 (2026-04-04)
Сделал:

Создал контроллеры для всех эндпоинтов

Написал роуты: auth.routes.ts, post.routes.ts, user.routes.ts

Подключил middleware authMiddleware к защищённым роутам

Ответ на свой вопрос:

Контроллер — самый тонкий слой: достаёт данные из req, вызывает сервис, отправляет res

Роуты группируют эндпоинты по ресурсам

Сложности:

Долго не мог понять, почему req.user — undefined (забыл вызвать authMiddleware)

День 7 (2026-04-05)
Сделал:

Написал errorHandler и errorMap

Создал requireAuth утилиту

Все ошибки теперь возвращают правильные HTTP статусы

Ответ на свой вопрос:

errorMap = словарь "сообщение ошибки → HTTP статус"

Не нужно писать if (error.message === '...') в каждом контроллере

Сложности:

Ошибка 500 вместо 404 — забыл кинуть new Error('Post not found') в сервисе

День 8 (2026-04-06)
Сделал:

Добавил лайки: likeRepository, likeService, likeController

Роут POST /api/posts/:postId/like

Счётчик лайков в посте (incrementLikes/decrementLikes)

Ответ на свой вопрос:

Лайк — это отдельная таблица (user, post)

Toggle — если лайк есть → удаляем, если нет → создаём

Сложности:

Не знал, как сделать связь many-to-many в Prisma (через отдельную модель Like)

День 9 (2026-04-07)
Сделал:

Добавил подписки: followRepository, followService, followController

Роут POST /api/users/:followeeId/follow

Нельзя подписаться на себя

Ответ на свой вопрос:

Подписка — тоже many-to-many (follower → followee)

Аналогично лайкам, но без счётчика

Сложности:

Запутался, кто на кого подписывается (followerId → followeeId)

День 10 (2026-04-08)
Сделал:

Добавил ленту: postRepository.getFeed()

JOIN через подзапрос: посты только тех, на кого подписан пользователь

Пагинация через cursor (а не offset)

Ответ на свой вопрос:

Cursor пагинация: WHERE id > lastId — быстро даже на 100к записей

Offset пагинация: OFFSET 10000 — медленно, БД сканирует все 10к записей

Сложности:

Не понял сразу разницу между cursor и offset (перечитал 3 раза)

День 11 (2026-04-09)
Сделал:

Написал интеграционные тесты для auth, posts, likes, follows

Тесты проходят по отдельности (все 24)

Ответ на свой вопрос:

Интеграционные тесты проверяют реальное API (HTTP запросы)

beforeEach — чистит БД перед каждым тестом

cleanupDatabase() удаляет все записи в правильном порядке (лайки → подписки → посты → юзеры)

Сложности:

postRes.body.id был undefined → сервер возвращал пост, но поле называлось по-другому

Тесты падают вместе → нужно maxWorkers: 1

День 12 (2026-04-10)
Сделал:

Установил ioredis, настроил подключение

Кэширование getFeed в Redis (TTL 30 секунд)

Инвалидация кэша при создании нового поста

Ответ на свой вопрос:

Redis в 100 раз быстрее PostgreSQL (RAM vs Disk)

TTL — время жизни кэша (компромисс между скоростью и актуальностью)

Инвалидация — удаляем старый кэш, когда данные изменились

Сложности:

Redis не стартовал → забыл запустить docker-compose up -d redis

ECONNREFUSED — ошибка подключения → проверил порт 6379

День 13 (2026-04-11)
Сделал:

Настроил Socket.IO сервер на том же порту 3000

Комнаты: socket.join('user:userId') для таргетинга уведомлений

Уведомления при лайках и подписках

Ответ на свой вопрос:

WebSocket — постоянное соединение, сервер может отправлять данные сам

Комнаты — отправка уведомления конкретному пользователю

Socket.IO в тестах отключён (NODE_ENV !== 'test')

Сложности:

(global as any).io → работает, но лучше сделать getIo()

В тестах Socket.IO падал → добавил проверку окружения